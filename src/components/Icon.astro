---
let { name, width, height, color } = Astro.props

if (!color) {
  color = 'currentColor'
}

if (!height) {
  height = width
}

if (isNum(width)) {
  width = `${width}px`
}

if (isNum(height)) {
  height = `${height}px`
}

function isNum(value) {
  return !isNaN(value)
}
---

<iconify-icon icon={name} width="none" style={{ width: `${width}`, height: `${height}`, color: `${color}` }}></iconify-icon>
